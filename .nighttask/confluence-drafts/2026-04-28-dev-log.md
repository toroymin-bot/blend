# Blend 개발일지 — 2026-04-28 (새벽 nighttask)

**작업자**: 꼬미 (Claude Code, blend-daily-dev)
**환경**: blend.ai4min.com (production)
**Confluence parent**: 9371649
**커밋**: [`d493aca`](https://github.com/toroymin-bot/blend/commit/d493aca)
**배포**: <https://blend-m4suncmil-toroymin-bots-projects.vercel.app> (READY, build 35s)

---

## 📌 오늘의 핵심

1. **Phase 4.1 — "다른 AI로" 재생성** 검증 완료. 4월 25일 BACKLOG에서 "Phase 4.0에서 toast 폴백으로 구현됨, 실제 인라인 픽커 + 재생성 로직은 Phase 4.1로 이월" 으로 잡혀 있던 항목인데, 코드 점검 결과 실제 인라인 픽커 + `regenerateAssistantMessage` + closure-safe `nextModelOverrideRef` 모두 이미 구현된 상태였음. 잔재 dead 카피 `comingSoon` 3 site만 정리.
2. **QA 다각도** — Phase 1 TC 100%(295/295), Phase 2 메뉴 30항목, Phase 3 시스템 10항목 모두 통과. 단, Phase 2 도중 카피 회귀 1건과 i18n 키 차이 1건을 발견 → 다음 nighttask 우선 처리.
3. STEP 0-A/0-B 스캔: Improvement 승인 대기, TC Fail 미수정, Bug Fix Requested 모두 0건.

---

## ✅ Phase 4.1 — "다른 AI로" 재생성 (closed)

### 배경
2026-04-25 Phase 4.0 작업 시 inline 모델 미니드롭다운 + 재생성 플로우는 시간 부족으로 `toast("곧 지원됩니다")` 폴백으로 처리하고, 실 구현을 Phase 4.1로 이월했었음. 오늘 점검 결과 이후 nighttask에서 실제 구현이 들어갔으나 BACKLOG 마킹이 누락되어 있었음.

### 구현 위치 (이미 wired 상태 확인)
- `src/modules/chat/chat-view-design1.tsx`
  - line 718 `regenerateAssistantMessage(assistantMsgId, newModel?)` — assistant 메시지 id 기준 직전 user 메시지 찾고, 그 이후 모두 잘라낸 뒤 `nextModelOverrideRef.current = newModel` + `setCurrentModel(newModel)` → `setTimeout(() => performSend(...), 0)` 으로 재생성
  - line 312 `nextModelOverrideRef = useRef<string | null>(null)` + line 842 `const effectiveModel = nextModelOverrideRef.current ?? currentModel; nextModelOverrideRef.current = null;` — closure-safe override
  - line 1816~1851 `MessageBubble` 안의 인라인 픽커 — `MODELS.filter((m) => m.id !== 'auto').slice(0, 8)`, hover에서 0→60% 노출, 선택 시 `onTryAnother(m.id)`
  - line 1333 `onTryAnother={(newModel) => regenerateAssistantMessage(msg.id, newModel)}` — D1MessageRow → MessageBubble 콜백 wiring

### 정리(dead-code 제거)
- ko/en `copy` 객체에서 `comingSoon: '곧 지원됩니다' / 'Coming soon'` 항목 삭제
- `CopyObj` type에서 `comingSoon: string` 필드 삭제
- 영향: 화면 변경 0, 번들에서 미사용 string literal 제거

### 재테스트 필요 (Roy)
production 채팅 → assistant 메시지에 마우스 hover → 우측 "↻ 다른 AI로" 노출 → 클릭 시 인라인 모델 드롭다운 → 다른 모델 선택 → 직전 user 질문이 새 모델로 재실행되는지 확인.

---

## 📋 D1-Page-07 추가 디자인 메모 (대조 결과)

`~/Downloads/Meeting_2026-04-25_v1.md` (Roy 추가) vs `~/Downloads/files (4)/Meeting_2026-04-25_v1.md` (이전) diff:

- 변경의 본질: **YouTube 링크 입력 UI v1에서 비활성화** (코드 골격은 유지, 향후 활성화 가능하도록)

`src/modules/meeting/meeting-view-design1.tsx` 점검 결과:
- `ytUrl`/`setYtUrl` state(line 334) + `fetchYoutubeTranscript`(line 319) + analyze 분기(line 454) — **코드 골격 보존됨**
- 입력 UI 영역(`InputPhase` line 633~) — textarea + 음성 파일 업로드만, **YouTube input 컴포넌트 미노출** ✅

→ 새 디자인 명세대로 이미 구현된 상태. 추가 작업 없음.

---

## 🧪 QA 결과

### Phase 1 — Test Checklist 진행률
| 지표 | 값 |
|---|---|
| 총 TC | 295 |
| 테스트 완료 (L열 채워진 row) | 295 |
| 미테스트 | 0 |
| 진행률 | **100%** |

추가로 진행할 row 없음. 신규 TC 추가 시점부터 다시 적재 시작.

### Phase 2 — 메뉴 다각도 코드 검증 (3 메뉴 × 10항목 = 30)

| 메뉴 | 항목 | 결과 |
|---|---|---|
| 채팅 (`chat-view-design1.tsx`) | onTryAnother 인라인 픽커 / regenerateAssistantMessage / nextModelOverrideRef closure-safe / dead 카피 0 / suggestedModel 매핑 / mobile sidebar / trial 배지 nowrap / CitationBlock RAG / safeSetItem store / formatUSD 사용 | **10/10 Pass** |
| 결제 (`billing-view-design1.tsx`) | D1BillingView 라우팅 / 새 카피 head·sub 통일 / 도발·이모지 제거 / LimitRow·ToggleRow / SVGLineChart / d1:billing-limit / 일일 한도 default / mode='savings' CostSavings 라우팅 / 자체 fmtMoney+KRW 변환 / 새 컨셉 카피 통일 | **10/10 Pass** (*formatUSD 0건은 자체 `fmtMoney` 사용 — 의도된 디자인*) |
| 모델 (`models-view-design1.tsx`) | D1ModelsView 라우팅 / 5 프로바이더 그룹 / FilterChip+ModelCard / REGISTRY_GENERATED_AT / use-case 구체적 설명 다수 / BRAND_COLORS 매핑 / safeSetItem | **9/10 Pass** (*model-registry 회귀 1건 — 아래 발견 사항*) |

### Phase 3 — 시스템 10항목

| # | 항목 | 결과 |
|---|---|---|
| 1 | Vercel 빌드 로그 에러/warning 0 | ✅ |
| 2 | localStorage 키 prefix 일관성 (bare 사용 0) | ✅ |
| 3 | i18n ko/en 키 균형 | ⚠️ ko 993 vs en 987 (Δ6) |
| 4 | suppressHydrationWarning 5 파일 적용 (BUG-003 회귀 방지) | ✅ |
| 5 | safeSetItem 12 파일 도입 (BUG-005) | ✅ |
| 6 | API 라우트 = stub (output:'export' 정책) | ✅ |
| 7 | StorageQuotaToast 메인+design1 양쪽 mount | ✅ |
| 8 | design1 D1*View 31 라우팅 일관 | ✅ |
| 9 | HTTP 200 — KO/EN qatest, 메인, design1 8 URL 모두 | ✅ |
| 10 | git status 정리됨 (관련 파일만 staged) | ✅ |

---

## 🚨 다음 nighttask 우선 처리 (BACKLOG carry-over)

### REG-01 — `model-registry.ts` "최신/최강" 추상 카피 15건 회귀
- 위치: line 137, 485, 617, 677, 749, 785, 972, 996, 1008, 1145, 1241, 1301, 1325, 1385, 1421
- 사례: "GPT-5.4 — 더 빠르고 정확해진 최신 AI", "어려운 코딩도 거뜬한 GPT-5.1 최강판", "긴 문서·복잡 분석 최강", "구글 최신 Gemma 4세대 AI", "빠르고 저렴한 Gemini 최신형"
- TC-FAIL-047(2026-04-22)에서 한 번 fix됐던 영역인데, 이후 `scripts/sync-models.ts`가 신규 모델을 등록하면서 자동생성된 description이 다시 추상 형용사를 사용하는 패턴으로 들어옴
- 다음 nighttask 작업: (1) 15건 모두 use-case 표현으로 교체, (2) `scripts/sync-models.*`에 카피 정규화 단계 추가

### i18n-DELTA-01 — ko/en 키 차이 6건
- ko 993 vs en 987 → 누락 키 추출 후 양쪽 동기화

---

## 🔁 4단계 프로세스 체크리스트

| # | 항목 | 결과 |
|---|---|---|
| ① | git commit + push (`d493aca`) | ✅ |
| ② | Blend_QA_Task.xlsx Dev 시트 row 125 기록 | ✅ |
| ③ | Confluence 개발일지 (이 페이지) | 🔵 발행 진행 |
| ④ | GAS 이메일 sendDevReport | 🔵 다음 단계 |

---

## 🗒 메모

- 컨텍스트 여유로 "최후 30분 보장 룰" 위반 없이 STEP 10 진입
- Phase 4.1 closed로 BACKLOG 미완료 항목은 GAS-AUTH(사용자 작업) 1건만 남음
- 컴포넌트 검증은 코드 정적 분석 + HTTP 200 + 빌드 로그 기반. 인라인 픽커 실제 동작은 Roy 검증 필요.
